import React from 'react';
import ReactDOM from 'react-dom';
import { Remote } from '../dist/remote.esm';

const App = () => {
  return (
    <h1>App</h1>
  )
};

ReactDOM.render(<App />, document.getElementById('root'));
