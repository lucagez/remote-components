{
  "name": "@remote-components/template",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Luca Gesmundo",
  "license": "MIT",
  "scripts": {
    "dev:dummy": "microbundle watch --cwd ./components/dummy --jsx React.createElement --format umd",
    "build:dummy": "microbundle --cwd ./components/dummy --jsx React.createElement --format umd",
    "serve": "serve ./components --cors"
  },
  "devDependencies": {
    "microbundle": "^0.12.0",
    "serve": "^11.3.1"
  }
}
