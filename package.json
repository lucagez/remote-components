{
  "name": "remote-components",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "run-p build:*",
    "test": "run-p test:*",
    "build:core": "cd packages/core && yarn build",
    "build:react": "cd packages/react && yarn build",
    "test:core": "cd packages/react && yarn test",
    "test:react": "cd packages/react && yarn test"
  },
  "workspaces": [
    "packages/*"
  ]
}
